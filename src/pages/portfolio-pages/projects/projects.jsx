import styles from './projects.module.css';

const Projects = () => {
    return (
        <div className={styles.container}>
            <h1>Projects</h1>
        </div>
    );
};

export default Projects;
